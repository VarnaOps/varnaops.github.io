# VarnaOps is in private preview. Nothing here is meant to be indexed yet.
#
# This is discouragement, not a control: a compliant crawler honours it, anyone with a
# URL is unaffected. The app itself is gated (basic auth today, invite-only accounts
# next); this file only keeps the marketing page and the attribution record out of
# search results while the product is not public.
#
# attribution.html NO LONGER LIVES HERE. On 2026-08-27 the apex moved to CloudFront and
# the canonical page moved with it, to https://varnaops.com/attribution.html, which is
# where the footer above now links. The licence obligation is discharged there: CC BY
# requires the credit be publicly reachable, and that path is exempt from the app's auth
# gate by design. Do not restore a copy here — two pages under one obligation is how the
# credit starts to differ from the data it credits.
User-agent: *
Disallow: /
